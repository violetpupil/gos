MySQL:
  Host: ${MYSQL_HOST}
  Port: ${MYSQL_PORT}
  # ""确保解析为字符串
  User: "${MYSQL_USER}"
  Pass: "${MYSQL_PASS}"
  Database: "${MYSQL_DATABASE}"
RedisC:
  Addr: ${REDIS_ADDR}
  Pass: "${REDIS_PASS}"
  DB: ${REDIS_DB}
